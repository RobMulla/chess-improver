# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-json
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: detect-private-key
      - id: mixed-line-ending

  # Ruff - Modern Python linter AND formatter (replaces Black + isort + flake8)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.14
    hooks:
      # Format code (replaces Black)
      - id: ruff-format
      # Lint and fix issues (includes import sorting)
      - id: ruff
        args: ['--fix', '--exit-non-zero-on-fix']

  # Security checks
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.6
    hooks:
      - id: bandit
        args: ['-c', 'pyproject.toml']
        additional_dependencies: ['bandit[toml]']

  # Test coverage enforcement
  - repo: local
    hooks:
      - id: pytest-coverage
        name: pytest with coverage check
        entry: bash -c 'cd /Users/robmulla/Repos/chess-improver && source venv/bin/activate && pytest tests/ --cov=src --cov-fail-under=40 -q --tb=no'
        language: system
        pass_filenames: false
        always_run: false
        stages: [push]  # Only run on push, not every commit
